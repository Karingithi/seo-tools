# Add project environment variables here when needed.

# Optional: URL of the local Express proxy server (e.g. http://localhost:3001).
# When set, the Sitemap Checker, Hreflang Validator, and Robots.txt Validator
# will use it for server-side URL fetching instead of falling back to public CORS proxies.
# VITE_API_URL=http://localhost:3001
